%It allows to use background images.
\usepackage{eso-pic}

%It allows to include code.
\usepackage{listings}

%It allows to include space-separated names in \includegraphics.
\usepackage[space]{grffile}

%Graphics. State machine.
\usepackage{tikz}
\usetikzlibrary{arrows,automata,positioning}

%It allows to use smallest text in descriptions.
\usepackage[font=scriptsize]{caption}

%Strikeout using \sout{text}.
\usepackage{ulem}

%It allows to strikeout in math mode using \cancel{exp}.
\usepackage{cancel}

%It allows to use / for division.
\usepackage{nicefrac}

%It allows to use colors.
\usepackage{color}

\usepackage{tcolorbox}
\tcbuselibrary{listingsutf8}

%It allows to use colored cells.
\usepackage{colortbl}
%It re-draw cells over colors.
\usepackage{hhline}
\usepackage{adjustbox}

%\usepackage{placeins} alternative that allows to use H instead of h!.
\usepackage{float}

%Multiple rows.
\usepackage{multirow}

%It splits long links.
\usepackage{xurl}

%No indent.
\setlength\parindent{0pt}

%It allows to write in spanish. utf8 option avoid the use of \'a, etc.
\usepackage[latin1, utf8]{inputenc}
\usepackage[spanish]{babel}

%It enables cites hyperlink and links inside references.
%It sorts references in order of appearance.
\usepackage[hyperref=true,style=numeric,backend=biber,sorting=none]{biblatex}
%Custom colors.
\definecolor{keyword}{RGB}{206,54,12}
\definecolor{string}{RGB}{65,140,64}
\definecolor{background}{rgb}{255,253,247}
\definecolor{comment}{RGB}{63,110,135}

%Code style.
\lstdefinestyle{mystyle}{
    backgroundcolor=\color{background},
    commentstyle=\color{comment},
    keywordstyle=\color{keyword},
    numberstyle=\tiny,
    stringstyle=\color{string},
    basicstyle=\footnotesize,
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=-10pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=8,
    %It makes the code copyable.
    columns=fullflexible
}
\lstset{style=mystyle}

%Warning
\usepackage{csquotes}

%Symbol set.
\usepackage{amssymb}

%It allows to include mathematical equations.
\usepackage{amsmath}

%It allows to include images.
\usepackage{graphicx}

%Modify index, urls, cites colors.
\usepackage[colorlinks=true, linkcolor=black]{hyperref}
\hypersetup{colorlinks, citecolor=[rgb]{0,0.6,0}, urlcolor=[rgb]{0.07,0.24,0.35}}

%It allows to use page number, heading and footpage.
\usepackage{fancyhdr}

%Warning
\setlength{\headheight}{52pt}
